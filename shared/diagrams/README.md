# Shared Diagrams

这里存放可被章节正文、示例 README 或后续站点页面复用的图示资产。

## Files

- `rag-baseline-flow.md`: 第 5 章基础 RAG 流程图，解释“资料准备 -> 切分 -> 检索 -> 回答”。
- `rag-optimization-loop.md`: 第 6 章优化回路图，解释“改写查询 -> 检索 -> 重排 -> 压缩 -> 回答 -> 观察结果”的迭代关系。

## Format

- 当前采用 Markdown + Mermaid，便于直接嵌入文档站。
- 如果后续统一转成图片或 SVG，可以保留这里的 Mermaid 版本作为源码。
